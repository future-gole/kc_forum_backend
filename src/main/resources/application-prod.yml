spring:
  application:
    name: kc_forum
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/mybatis_test? characterEncoding=utf8&useSSL=falseallowPublicKeyRetrieval=true
    username: root
    password: Zz161011.
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 58080
mybatis-plus:
  configuration: # 配置打印 MyBatis⽇志
    map-underscore-to-camel-case: true #格式转化
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl